usage: sas_to_dta.py [-h] [-x] target_path

positional arguments:
  target_path           target path to transform, file or directory.

optional arguments:
  -h, --help            show this help message and exit
  -x, --extract_archive
                        determine to extract archive, only support zip.
